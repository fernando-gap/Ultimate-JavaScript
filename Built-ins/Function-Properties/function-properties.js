// Built-ins

/**
 * Function Properties
 * */


isFinite(testValue);
/*

Verify if the number passed is a
finite value or not

*/


isNaN(value);
/*

Verify if the number passed is a
NaN and return true or false.

*/


parseFloat(string);
/*

return a floating number, if the arg passed is a number,
the function will transform into a string if needed

*/


parseInt(string [, radix]);
/*

The function parses a string argument and returns an
integer of the specified radix (the base in mathematical
numeral systems).

*/


decodeURI(encodedURI);
/*

Decode a URI if is created by encodeURI or by
a similar.

 */


decodeURIComponent(encodedURI);
/*

The decodeURIComponent(); function decodes a Uniform
Resource Identifier (URI) component previously created by
encodeURIComponent or by a similar routine.

*/


encodeURI(URI);
/*

The encodeURI(); function encodes a URI by replacing
each instance of certain characters by one, two, three,
or four escape sequences representing the UTF-8 encoding
of the character (will only be four escape sequences for
characters composed of two "surrogate" characters).

*/


encodeURIComponent(string);
/*

Encode a component of an URI

*/
